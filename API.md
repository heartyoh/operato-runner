# Operato Runner API 문서

## 개요

Operato Runner의 REST API 문서입니다. 모든 API는 `/api` 접두사를 사용합니다.

## 인증

- **엔드포인트**: `/api/token`
- **메소드**: POST
- **설명**: 인증 토큰을 발급받습니다.

## 모듈 관리

### 모듈 목록 및 기본 작업

| 엔드포인트            | 메소드 | 설명                               |
| --------------------- | ------ | ---------------------------------- |
| `/api/modules`        | GET    | 모듈 목록을 조회합니다             |
| `/api/modules`        | POST   | 새로운 모듈을 생성합니다           |
| `/api/modules/{name}` | GET    | 특정 모듈의 상세 정보를 조회합니다 |
| `/api/modules/{name}` | DELETE | 특정 모듈을 삭제합니다             |
| `/api/modules/{name}` | PATCH  | 모듈 정보를 업데이트합니다         |

### 모듈 버전 관리

| 엔드포인트                       | 메소드 | 설명                              |
| -------------------------------- | ------ | --------------------------------- |
| `/api/modules/{name}/versions`   | POST   | 새로운 버전을 업로드합니다        |
| `/api/modules/{name}/rollback`   | POST   | 특정 버전으로 롤백합니다          |
| `/api/modules/{name}/activate`   | POST   | 특정 버전을 활성화합니다          |
| `/api/modules/{name}/deactivate` | POST   | 특정 버전을 비활성화합니다        |
| `/api/modules/{name}/history`    | GET    | 모듈의 버전 히스토리를 조회합니다 |

### 모듈 실행

| 엔드포인트        | 메소드 | 설명      |
| ----------------- | ------ | --------- |
| `/api/run/{name}` | POST   | 모듈 실행 |

### 환경 및 시스템 관리

| 엔드포인트          | 메소드 | 설명           |
| ------------------- | ------ | -------------- |
| `/api/environments` | GET    | 환경 목록 조회 |
| `/api/health/db`    | GET    | DB 상태 확인   |
| `/api/audit/logs`   | GET    | 감사 로그 조회 |

### 기타

| 엔드포인트                   | 메소드 | 설명           |
| ---------------------------- | ------ | -------------- |
| `/api/modules/{name}/deploy` | POST   | 모듈 배포      |
| `/api/modules/{name}/deploy` | DELETE | 모듈 배포 해제 |

## 파라미터 설명

### 경로 파라미터

| 파라미터 | 타입   | 설명                                |
| -------- | ------ | ----------------------------------- |
| name     | string | 모듈의 고유 이름(문자열, 중복 불가) |

### 쿼리 파라미터

| 파라미터 | 타입    | 설명                 | 기본값 |
| -------- | ------- | -------------------- | ------ |
| page     | integer | 페이지 번호          | 1      |
| limit    | integer | 페이지당 항목 수     | 20     |
| sort     | string  | 정렬 기준 필드       | id     |
| order    | string  | 정렬 방향 (asc/desc) | asc    |

## 응답 형식

모든 API 응답은 다음과 같은 기본 형식을 따릅니다:

```json
{
  "success": true/false,
  "data": {
    // 응답 데이터
  },
  "error": {
    "code": "에러 코드",
    "message": "에러 메시지"
  }
}
```

## 에러 코드

| 코드 | 설명                  |
| ---- | --------------------- |
| 400  | 잘못된 요청           |
| 401  | 인증 실패             |
| 403  | 권한 없음             |
| 404  | 리소스를 찾을 수 없음 |
| 500  | 서버 내부 오류        |

## 주의사항

1. 모든 요청에는 적절한 인증 토큰이 필요합니다.
2. API 버전 관리는 URL에 포함되지 않으며, 헤더를 통해 처리됩니다.
3. 모든 날짜/시간은 UTC 기준으로 처리됩니다.
4. 모듈 ID는 UUID v4 형식을 사용하여 고유성을 보장합니다.
5. name은 반드시 고유해야 하며, 특수문자/공백 사용 시 URL 인코딩 필요
